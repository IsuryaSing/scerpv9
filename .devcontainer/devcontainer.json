{
  "name": "scerpv9 Laravel Dev",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/scerpv9",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "20"
    }
  },
  "forwardPorts": [8000, 5173],
  "portsAttributes": {
    "8000": {
      "label": "Laravel App",
      "onAutoForward": "openPreview"
    },
    "5173": {
      "label": "Vite",
      "onAutoForward": "silent"
    }
  },
  "postCreateCommand": "composer install && npm install && cp -n .env.example .env && sed -i 's/^DB_HOST=.*/DB_HOST=db/' .env && sed -i 's/^DB_PORT=.*/DB_PORT=5432/' .env && sed -i 's/^DB_DATABASE=.*/DB_DATABASE=scerpv9/' .env && sed -i 's/^DB_USERNAME=.*/DB_USERNAME=scerpv9/' .env && sed -i 's/^DB_PASSWORD=.*/DB_PASSWORD=scerpv9_dev_password/' .env && sed -i 's/^DB_SCHEMA=.*/DB_SCHEMA=scerpv9/' .env && php artisan key:generate && php artisan config:clear",
  "postStartCommand": "nohup php artisan serve --host=0.0.0.0 --port=8000 > /tmp/scerpv9-laravel.log 2>&1 &",
  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "onecentlin.laravel-blade",
        "dbaeumer.vscode-eslint"
      ]
    }
  }
}
